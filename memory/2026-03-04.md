# 2026-03-04

- Fernando compartiÃ³ claves API por Telegram para stack de inversiones (Finnhub, FMP, CoinGecko, FRED, NewsAPI, Alpha Vantage).
- Por seguridad, NO se guardan secretos completos en memoria curada ni en chat.
- AcciÃ³n recomendada: rotar todas las claves y moverlas a .env local/gestor de secretos.
- Referencia de control: guardar solo Ãºltimos 4 caracteres por proveedor.

## 2026-03-04 17:40 — cierre de estado + backup + aprendizaje
- Se habilitó autenticación SSH funcional con GitHub para `agents_dashboard` usando clave local `id_ed25519_openclaw`; push confirmado.
- Se actualizó dashboard para estado real de APIs (OK/FALTA/ERROR) con probes vivos; FMP migrado a endpoint `stable` y funcionando.
- Se corrigieron problemas de claves con artefactos de pegado (`r`/`n`) en carga de entorno.
- Se implantó módulo de aprendizaje diario (`learning_daily.py`) con semáforo objetivo ROJO/AMARILLO/VERDE basado en métricas 7d (expectancy, profit factor, drawdown, win-rate).
- Se añadió visualización del semáforo en dashboard cripto y tarea programada diaria (`OpenClaw-Learning-Daily`).
- Se creó base LSTM v1 (scripts `train_lstm.py`, `predict_lstm.py`) como confirmador de señal, no decisor único.
- Se instalaron dependencias para LSTM (`numpy`, `torch+cpu`) y se validó entrenamiento/predicción en BTCUSDT.
- Se pidió y recibió compromiso del usuario para mantener explicaciones en lenguaje natural y con reporting de acciones antes/después.
- Backup manual completo generado en:
  `C:\Users\Fernando\.openclaw\workspace\backups\manual\full_state_backup_20260304_173946.zip`
