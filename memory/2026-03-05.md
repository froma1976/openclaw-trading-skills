## 2026-03-05 11:25 (Europe/Madrid)
- Fernando pidió estabilizar el sistema cripto y verificar que no hubiera datos congelados en dashboard.
- Se arreglaron tareas programadas que fallaban por rutas/ejecución en Windows y se reconfiguró refresco:
  - OpenClaw-Crypto-Ingest-2m
  - OpenClaw-Crypto-Scalp-1m
  - OpenClaw-Crypto-Stream-Probe-3m
- Se creó watchdog de frescura de datos (`audit_crypto_freshness.py`) con tarea `OpenClaw-Crypto-Watchdog-10m`.
- Se añadió alerta automática a Telegram cuando hay congelación/incidencia (con anti-spam por fingerprint de error).
- Se implementó cálculo en vivo para órdenes cripto activas: `current_price`, `pct_move`, `pnl_usd_est`.
- Se confirmó que el entrenamiento diario que existía no era LSTM; se implantó entrenamiento diario real LSTM y quedó operativo.
- Se montó pipeline histórico para entrenar en serio con datos reales:
  - Descarga Binance 5 años, 5m para BTCUSDT/ETHUSDT/SOLUSDT (`download_binance_history.py`).
  - Entrenamiento LSTM desde histórico multi-activo (`train_lstm_from_history.py`).
  - Script diario incremental + reentreno (`run_history_update_and_train_hidden.ps1`).
  - Tareas programadas diaria y semanal para actualización/rebuild histórico.
- Se resolvió error de memoria en entrenamiento histórico (OOM) cambiando a entrenamiento por lotes (DataLoader/batch).
- Métricas iniciales reales tras entreno histórico:
  - BTCUSDT val_mse: 2.77e-06
  - ETHUSDT val_mse: 4.9e-06
  - SOLUSDT val_mse: 6.06e-06
- Fernando expresó escepticismo sobre IA y pidió transparencia total para demostrar a terceros que el entreno es real.
- Se creó nueva pestaña de dashboard “LSTM real” (sin datos inventados) mostrando:
  - estado de entrenamiento en ejecución,
  - existencia de históricos/modelos,
  - velas reales por activo,
  - `trained_at`, `val_mse`, `dataset_points`,
  - últimas líneas del log real.

## 2026-03-05 18:02 (Europe/Madrid)
- Fernando confirmó prioridad actual: entrenar y robustecer LSTM; sin prisa por trading real con dinero.
- Se corrigió dashboard para órdenes cripto activas evitando PnL falso en 0.0:
  - si falta precio live -> `current_price/pct_move/pnl_usd_est = None` (N/A en UI),
  - cálculo de `pnl_usd_est` usando tamaño de posición (`qty/notional`) en vez de solo delta de precio.
- Se dejó ejecución en segundo plano para tareas de autopilot/scalp (sin ventanas emergentes de consola).
- Se dejó reentreno diario LSTM en 15m para BTC/SOL con pipeline ampliado:
  - actualización de histórico,
  - entrenamiento BTC + SOL,
  - reporte de aprendizaje diario,
  - QA de dataset,
  - evaluación walk-forward + baseline,
  - actualización de registry/champion.
- Se implantó estructura de gobernanza del modelo:
  - `config/risk.yaml` (riesgo, slippage, fees, pausas),
  - `models/registry.json` (historial + champion),
  - reportes automáticos (`walkforward_report.md`, `baseline_vs_lstm.csv`, `data_quality_report.json`).
- Se añadieron extractores de historial privado Binance (spot y futures) para múltiples símbolos; en la API actual devolvieron 0 operaciones en todos los pares consultados.
- Se creó catálogo v2 de fuentes públicas y reporte de cosecha (`public_sources_catalog_v2.csv`, `public-data-harvest-v2.md`).
- Se añadió ingestor público OHLCV desde CryptoDataDownload (BTC/SOL/ETH diarios) y quedó integrado en el ciclo diario.
- `train_lstm.py` ahora prioriza: histórico local -> OHLCV público -> API live (fallback resiliente).
- Blofin quedó pendiente de conexión efectiva: pruebas de solo lectura devuelven error de whitelist/IP en la API key actual; requiere terminar configuración correcta de permisos/whitelist en Blofin para validar saldo.
- Preferencias reforzadas de Fernando:
  - explicaciones claras en español,
  - pasos prácticos sin humo,
  - foco en calidad de datos, validación robusta y seguridad antes de operar en real.
